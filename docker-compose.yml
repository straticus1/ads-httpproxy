services:
  proxy:
    build: .
    image: ads-httpproxy:latest
    ports:
      - "8080:8080"
      - "9090:9090" # Admin API
      - "443:443/udp" # QUIC
    environment:
      - ADS_ADDR=:8080
      - ADS_API_ADDR=:9090
      - ADS_API_SECRET=${ADS_API_SECRET:?set ADS_API_SECRET before starting the stack}
      - ADS_ENABLE_QUIC=true
      - ADS_ENABLE_REUSEPORT=false # reuseport often fails in docker without host net
    restart: always
    networks:
      - proxy-net

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    networks:
      - proxy-net

  prometheus:
    image: prom/prometheus
    volumes:
      - ./docker/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9091:9090"
    networks:
      - proxy-net

networks:
  proxy-net:
    driver: bridge
